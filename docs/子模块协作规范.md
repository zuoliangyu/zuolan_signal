# 子模块协作规范

## 1. 根仓库与子模块的职责

### 根仓库负责

- 子模块接入与路径管理
- 方案总览与导航
- 跨方案规则
- 版本映射与集成记录

### 子模块负责

- 具体方案代码
- 本方案 `AGENTS.md`
- 本方案文档、构建说明、验证说明
- 本方案独立提交与版本演进

## 2. 新增子模块的基本流程

1. 先准备独立仓库
2. 在根仓库执行 `git submodule add <repo-url> <path>`
3. 提交 `.gitmodules` 和子模块目录指针
4. 更新 `README.md`、`docs/README.md`、`docs/方案总览.md`

## 3. 更新已有子模块的流程

1. 进入子模块目录完成开发与提交
2. 推送子模块仓库
3. 回到根仓库提交新的子模块指针
4. 如有路径、接口、流程、脚本变化，同步更新相关文档

## 4. 当前 H7 的特殊状态

当前 `H7` 在接入初期先使用本地仓库路径。

这意味着：

- 本机可以直接联动开发
- 推送总控仓库到 GitHub 前，需要先把 `H7` 自身推到 GitHub
- 之后再把根仓库 `.gitmodules` 中的 `H7` URL 改成正式远程地址

## 4.1 当前 F4_FPGA 的特殊状态

当前 `F4_FPGA` 在接入初期也先使用本地仓库路径。

这意味着：

- 本机可以直接联动开发
- 推送总控仓库到 GitHub 前，需要先把 `F4_FPGA` 自身推到 GitHub
- 之后再把根仓库 `.gitmodules` 中的 `F4_FPGA` URL 改成正式远程地址
- 当前还额外归档了一份历史参考代码压缩包到 `F4_FPGA/old_code.zip`

## 5. AGENTS 规则分层

### 根目录 `AGENTS.md`

- 只处理总控层规则
- 不写死具体芯片方案的构建或烧录命令

### 子模块 `AGENTS.md`

- 处理本方案技术栈、目录、验证、文档要求
- 保证单独打开子模块仓库时也能独立工作

## 6. 禁止事项

- 不要把子模块目录加入根仓库 `.gitignore`
- 不要只在根仓库写关键方案规则而不在子模块补齐
- 不要在根仓库长期堆放应属于子模块的大量源码

## 7. 当前 H7 迁移注意事项

- 当前 `H7` 已作为子模块目录接入总控仓库。
- 但 `H7` 子仓库内部仍保留了你原本未提交的迁移状态：
  - 已删除旧工程目录 `zuolan_signal_STM32/`
  - 已新增新工程目录 `zuolan_signal_STM32_H7/`
- 这意味着当前总控仓库里的 `H7` 是“可继续开发”的状态，但还不是“干净的正式子模块版本点”。

建议后续顺序：

1. 先在 `H7/` 内整理并提交这次迁移
2. 把 `H7` 推到 GitHub
3. 回到根仓库把 `.gitmodules` 的 `H7` URL 改成正式远程地址
4. 再在根仓库提交新的子模块指针

## 8. 当前 F4_FPGA 迁移注意事项

- 当前 `F4_FPGA` 已作为子模块目录接入总控仓库。
- `F4_FPGA` 子仓库当前保留了源仓库工作树中的本地改动与未跟踪文件：
  - 已保留源仓库中的未跟踪文档 `Doc/开发/Untitled.md`
  - 已新增旧代码归档压缩包 `old_code.zip`
- 这意味着当前总控仓库里的 `F4_FPGA` 也是“可继续开发”的状态，但还不是“干净的正式子模块版本点”。

建议后续顺序：

1. 先在 `F4_FPGA/` 内整理并提交当前改动与 `old_code.zip`
2. 把 `F4_FPGA` 推到 GitHub
3. 回到根仓库把 `.gitmodules` 的 `F4_FPGA` URL 改成正式远程地址
4. 再在根仓库提交新的子模块指针
